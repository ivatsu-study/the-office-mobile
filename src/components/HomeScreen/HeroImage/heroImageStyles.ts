import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  imageContainer: {
    width: '100%',
    height: 200,
    aspectRatio: 1 * 1.8,
  },
  image: {
    width: '100%',
    height: '100%',
    resizeMode: 'cover',
  },
})

export default styles
