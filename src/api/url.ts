const API_URL = 'https://officeapi.dev/api';

export const QUOTES = `${API_URL}/quotes`;
export const CHARACTERS = `${API_URL}/characters`;
export const CREW = `${API_URL}/crew`;
export const EPISODES = `${API_URL}/episodes`;
